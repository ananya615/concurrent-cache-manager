# concurrent-cache-manager
A Concurrent Cache Manager: Implement a fixed-size in-memory cache (key-value store) that can be accessed by multiple threads. Use reader-writer locks (pthread_rwlock) to allow concurrent reads but exclusive access for writes and cache evictions.
